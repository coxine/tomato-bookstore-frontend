import { Typography } from '@mui/joy'

export default function Loading() {
  return (
    <Typography
      level="h4"
      sx={{
        textAlign: 'center',
        mt: 4,
      }}
    >
      加载中...
    </Typography>
  )
}
